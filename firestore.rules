rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isAdmin() {
      return request.auth != null &&
        request.auth.token.email in [
          "julianotimoteo@usinapitangueiras.com.br",
          "wallasonsoares@gmail.com"
        ];
    }

    function isAuthenticated() {
      return request.auth != null;
    }

    match /artifacts/{appId}/public/data {
      match /settings/config {
        allow read: if isAuthenticated();
        allow write: if isAdmin();
      }

      match /pending_approvals/{solicitationId} {
        allow create, read: if true;
        allow update, delete: if isAdmin();
      }

      match /users/{email} {
        allow read: if isAuthenticated();
        allow create: if request.auth != null && email == request.auth.token.email;
        allow update: if request.auth != null && email == request.auth.token.email;
        allow delete: if isAdmin();
      }

      match /{document=**} {
        allow read: if isAuthenticated();
        allow write: if isAdmin();
      }
    }

    match /{path=**} {
      allow read, write: if false;
    }
  }
}
